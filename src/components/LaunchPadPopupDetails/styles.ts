import styled from "styled-components";

const Wrapper = styled.div``;
const ReadonlyField = styled.div`
  display: flex;
  align-self: center;
`;
const Label = styled.span`
  font-weight: 500;
  margin-right: 5px;
`;
const Value = styled.span``;

export { Label, ReadonlyField, Value, Wrapper };
